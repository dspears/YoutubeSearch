(function (window) {
  window.__env = window.__env || {};
  // Add your API Key in the line below.  Make sure you have Youtube Data API enabled in Google API Console.
  window.__env.apiKey = 'API Key Goes Here';
}(window));
